{% extends 'base.html.twig' %}

{% block title %}Liste des acteurs
{% endblock %}


{% block body %}
	<h1 class="border border-dark rounded p-2 m-2 text-white bg-primary">

		Acteurs

	</h1>
	<style>
		.flexbox-container {
			display: -ms-flex;
			display: -webkit-flex;
			display: flex;
			flex-wrap: wrap;
			flex-direction: row;
			justify-content: flex-start;
			align-items: stretch;
			width: 25%;
		}

		.flexbox-container > div {
			width: 50%;
			margin: 0 10px;
			flex: 1;
			padding: 10px;
		}
	</style>

	<center>
		<div class="flexbox-container">			
				<div>
					<img style="width:100px;" src="{{ asset(actor.image) }}"><br>
					<h3>{{ actor.prenom }}
						{{ actor.nom }}</h3><br>
				</div>			
		</div>
{{ form_start(form) }}

{# {{form_widget(form)}} #}
<div>{{ form_row(form.nom, {"attr" : {'class':'text-dark bg-success'},"label":"Nom : "})}}</div>
<div>{{ form_row(form.prenom, {"attr" : {'class':'text-dark bg-success'},"label":"Prénom : "})}}</div>
<div>{{ form_row(form.image, {"attr" : {'class':'text-dark bg-success'},"label":"Image : "})}}</div>
<input type="submit" value="enregistrer">
{{ form_end(form) }}

	</center>
{% endblock %}

